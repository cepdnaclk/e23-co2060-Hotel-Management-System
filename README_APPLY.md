# TourismHub LK – Footer Only Fix Phase 42

This update keeps the full-screen landing hero and only improves the landing footer layout.

## Changed file
- `client/src/pages/HomePage.jsx`

## Apply
Copy the `client` folder into your project root and choose Replace files.

## Test
```bash
cd client
npm run dev
```

## Commit
```bash
git status
git add client/src/pages/HomePage.jsx
git commit -m "Fix landing footer full width design"
git push origin dev
```
