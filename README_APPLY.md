# Navbar Phase 40 - Mobile Hamburger Menu

This update replaces only:

client/src/layouts/PublicLayout.jsx

It restores the mobile hamburger menu button. On mobile, the navbar is collapsed by default and opens when the three-line button is clicked.

Apply:
1. Extract the ZIP.
2. Copy the `client` folder into your project root.
3. Choose Replace files.
4. Run `cd client && npm run dev`.
